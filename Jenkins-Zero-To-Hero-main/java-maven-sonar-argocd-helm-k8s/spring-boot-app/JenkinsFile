pipeline {
  agent {
    docker {
      image 'maven:3.9.6-eclipse-temurin-17'
      args '--user root -v /var/run/docker.sock:/var/run/docker.sock'
    }
  }

  environment {
    PROJECT_DIR = "Jenkins-Zero-To-Hero-main/java-maven-sonar-argocd-helm-k8s/spring-boot-app"
    MANIFEST_FILE = "Jenkins-Zero-To-Hero-main/java-maven-sonar-argocd-helm-k8s/spring-boot-app-manifests/deployment.yml"
  }

  stages {
    stage('Checkout') {
      steps {
        checkout scm
        sh 'ls -ltr'
      }
    }

    stage('Tool Versions') {
      steps {
        sh 'java -version'
        sh 'mvn -version'
      }
    }

    // ✅ NEW: Install Docker CLI so "docker build" works inside this container
    stage('Install Docker CLI') {
      steps {
        sh '''
          set -e
          if command -v docker >/dev/null 2>&1; then
            echo "Docker CLI already present"
            docker --version
          else
            echo "Installing Docker CLI..."
            apt-get update
            apt-get install -y docker.io
            docker --version
          fi
        '''
      }
    }

    stage('Build and Test') {
      steps {
        dir("${PROJECT_DIR}") {
          sh 'mvn clean package'
        }
      }
    }

    stage('Static Code Analysis') {
      environment {
        SONAR_URL = "http://15.207.237.18:9000"
      }
      steps {
        withCredentials([string(credentialsId: 'sonarqube', variable: 'SONAR_AUTH_TOKEN')]) {
          dir("${PROJECT_DIR}") {
            sh 'mvn sonar:sonar -Dsonar.login=$SONAR_AUTH_TOKEN -Dsonar.host.url=$SONAR_URL'
          }
        }
      }
    }

    stage('Build and Push Docker Image') {
      environment {
        DOCKER_IMAGE = "avinash27112002/ultimate-cicd:${BUILD_NUMBER}"
      }
      steps {
        script {
          dir("${PROJECT_DIR}") {
            sh 'docker version'
            sh 'docker build -t ${DOCKER_IMAGE} .'
          }

          def dockerImage = docker.image("${DOCKER_IMAGE}")
          docker.withRegistry('https://index.docker.io/v1/', "docker-cred") {
            dockerImage.push()
          }
        }
      }
    }

    stage('Update Deployment File') {
      environment {
        GIT_REPO_NAME = "Jenkins-Zero-To-Hero"
        GIT_USER_NAME = "avinashneela"
      }
      steps {
        withCredentials([string(credentialsId: 'github', variable: 'GITHUB_TOKEN')]) {
          sh """
            git config user.email "avinashneela27@gmail.com"
            git config user.name "Avinash"

            sed -i "s/replaceImageTag/${BUILD_NUMBER}/g" ${MANIFEST_FILE}

            git add ${MANIFEST_FILE}
            git commit -m "Update deployment image to version ${BUILD_NUMBER}" || true

            git push https://${GITHUB_TOKEN}@github.com/${GIT_USER_NAME}/${GIT_REPO_NAME}.git HEAD:main
          """
        }
      }
    }
  }
}
